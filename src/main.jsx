import React from 'react'
import ReactDOM from 'react-dom/client'
import PricingSimulator from '../PricingSimulatorV2.jsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <PricingSimulator />
  </React.StrictMode>,
)
